let fruits = ["Apple", "Mango", "Orange", "Avacado"];



const filter = () => {
  for (let i = 0; i < fruits.length; i++) {
    if (fruits[i].charAt(0) == "A") {
       console.log(fruits[i]); // i used return here but if used return it
    } else {
        
    }
  }
};

filter();







